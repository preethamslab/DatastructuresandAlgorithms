package com.algoexpert.arrays;

public class LinkedListTest
{
    // linked list stores anywhere in memory
    // node, value to pointer to next node
    //  get -> O(i) T, O(1) S
    // set -> O(i) T, O(1) S
    // init -> O(N) ST
    // copy => O(N) ST
    // insert -> insert anywhere -> O(i) i would be the index and update the references.
    // if insert operation, inserting is O(1) operation, but finding a location is O(i)
    // operation
    // double linked list , 3 <> 1 <> 4 <> 7
    // single 3 ->1 -> 4 -> 7


}
