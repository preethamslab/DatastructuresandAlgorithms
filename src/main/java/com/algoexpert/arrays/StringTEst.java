package com.algoexpert.arrays;

public class StringTEst
{
     /*
        String traverse: O(N) T, O(1)S
        COPY: O(N) ST
        GET: O(1) ST
        Strings are immutable
        every alteration, is copying to new string O(N)
        SET : immutable is string, we can't set

      */
}
